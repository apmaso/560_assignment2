module fpv_assume(
    input logic clk,
    input logic rst_b,
    input req_pkt_type input_req
	);

// If fifo_full then must have !input_req.req
property fifo_full;
@(posedge clk) 
(fifo_full |-> !input_req.req);
endproperty

assume_fifo_full:assume property(fifo_full);

endmodule
